# تدقيق شامل لمِداد القاضي

> جرد كامل قبل أي إصلاح، بحسب نطاق التدقيق الثلاثيني. كل بند مذكور ولو كان سليمًا.
> المرجع: `HANDOFF.md` ثم `SPEC.md`. القواعد اللغوية القضائية والتصميم لم تُمس.
> التاريخ: ٨ سبتمبر ٢٠٢٦ — الحالة عند الجرد: `npm test` ٦٥ اختبارًا ناجحًا، البناء نظيف.

## ملخص

| الخطورة | عدد | الحالة |
|---|---|---|
| حرجة | ٣ | أُصلحت كلها |
| عالية | ٩ | أُصلحت كلها |
| متوسطة | ١٤ | أُصلح ١٢، أُجّل ٢ |
| منخفضة | ١٠ | مؤجّلة مع اقتراح |
| سليم (فُحص ولم يُوجد خلل) | ١٨ بندًا من نطاق التدقيق | — |

---

## أولًا: الخلل المكتشف

| # | الملف والسطر | الوصف | الخطورة | الإصلاح | الحالة |
|---|---|---|---|---|---|
| 1 | `app.src.jsx:606-607` | الحفظ المؤجَّل ٦٠٠ مللي ثانية لا يُفرَّغ عند إغلاق الصفحة أو تصغير التطبيق. في سفاري الجوال يُجمَّد المؤقّت عند الخروج من التبويب، فتضيع آخر ملاحظة كتبها القاضي بلا أثر. | **حرجة** | تفريغ فوري عند `pagehide` و`visibilitychange`، وحفظ متزامن عند إخفاء الصفحة | أُصلح — `4c912b7` |
| 2 | `app.src.jsx:58-63` | `DB.open()` يخزّن الوعد؛ فإن فشل فتح IndexedDB (سفاري خاص، ذاكرة ممتلئة) يبقى الوعد مرفوضًا للأبد، و`sget/sset` يبتلعان الخطأ بصمت. النتيجة: التطبيق يعمل ويبدو سليمًا **وكل شيء يُكتب في الفراغ**. | **حرجة** | إعادة المحاولة عند الفشل بدل تثبيت الرفض، وراية `DB.broken` تُعرض للمستخدم شريطًا أحمر صريحًا | أُصلح — `2443aca` |
| 3 | `server.js:115-116` | `handleRelay` لا يطلب كلمة مرور الموقع. أي شخص على الإنترنت يرسل `x-midad-provider` + مفتاحه فيستعمل خادمك **وسيطًا مجهولًا** إلى OpenAI وغيره. الحد ٣٠ طلبًا/دقيقة لكل IP لا يمنع ذلك. | **حرجة** | اشتراط `x-midad-pass` للتمرير ما دام الخادم يحمل كلمة مرور مضبوطة | أُصلح — `2276920` |
| 4 | `app.src.jsx` (لا وجود له) | لا `ErrorBoundary`. أي خطأ تصيير واحد يترك **صفحة بيضاء** بلا رسالة ولا مخرج، والقضايا كلها في المتصفح. | **عالية** | `ErrorBoundary` عربي يعرض سبب العطل وزر «أعد تحميل الصفحة» وزر «ارجع إلى الرئيسية» | أُصلح — `e04ac39` |
| 5 | `app.src.jsx:362-373` | `extractPdfText` يضم عناصر النص بترتيب ورودها بلا إحداثيات. في PDF عربي كثير يخرج النص **مبعثرًا ومقلوبًا** («هـ الساعة /1445 /4 25في يوم الخميس»)، ويُرسل كأنه سليم فيبني عليه التحليل. | **عالية** | كشف بسيط لجودة الاستخراج (نسبة الكلمات المفككة وعكس ترتيب الأرقام)، وعند الشك يُهمَل النص ويُرسل الملف الأصلي، مع إخبار القاضي | أُصلح — `85e6a91` |
| 6 | `app.src.jsx:737-780` | لا سبيل لإلغاء تحليل جارٍ. المراحل الست تمضي إلى آخرها ولو أدرك القاضي أنه رفع الملف الخطأ. | **عالية** | `AbortController` مشترك، وزر «أوقف التحليل» في شاشة التقدم | أُصلح — `d190c54` |
| 7 | `app.src.jsx:1125-1137` | مرحلة تفشل فلا يوجد إلا «أعد التحليل» الذي يعيد الست كلها ويدفع ثمنها كلها. | **عالية** | `runStages` تقبل `only` بمراحل بعينها، وزر «أعد هذه المرحلة وحدها» بجانب كل مرحلة فاشلة | أُصلح — `d190c54` |
| 8 | `app.src.jsx:1230` | أزرار «تعديل» و«حذف» على بطاقة المسألة داخل `hidden group-hover:flex`. لا `hover` في اللمس، فالقاضي **لا يستطيع تعديل مسألة ولا حذفها من الجوال** — وهو يعمل من الجوال غالبًا، والتعديل مطلوب في `SPEC.md`. | **عالية** | إظهارها دائمًا على الشاشات الصغيرة | أُصلح — `f757f49` |
| 9 | `app.src.jsx:86-95` | `dual()` تفترض أن `Intl` سيرفع استثناءً إن لم يدعم أم القرى. لا يرفع: **يسقط بصمت إلى تقويم آخر**، فيُعرض تاريخ هجري غير أم القرى على أنه أم القرى. خطأ تاريخ في وثيقة قضائية. | **عالية** | التحقق من `resolvedOptions().calendar`، وإخفاء الهجري إن لم يكن `islamic-umalqura` | أُصلح — `f757f49` |
| 10 | `server.js:79,121` | مفتاح الحد من الطلبات يؤخذ من **أول** قيمة في `x-forwarded-for`، وهي التي يتحكم فيها العميل. تزييفها يلغي الحد بالكامل. | **عالية** | أخذ **آخر** قيمة (التي يضيفها وسيط Render الموثوق) | أُصلح — `d803e25` |
| 11 | `app.src.jsx:806,941` | نصّان يقولان للقاضي إن الملفات «تُرسل مباشرة من متصفحك إلى Anthropic API بمفتاحك». هذا **غير صحيح** في وضع كلمة مرور الموقع (تمر بالخادم) ولا مع المزوّدين الآخرين. تصريح خصوصية مغلوط. | **عالية** | نص يذكر الوجهة الفعلية بحسب الوضع والمزوّد المختار | أُصلح — `d190c54` |
| 12 | `server.js` (لا وجود له) | لا `Content-Security-Policy`. | متوسطة | سياسة تسمح بـ cdnjs وtailwind وخطوط جوجل وعناوين المزوّدين فقط | أُصلح — `d803e25` |
| 13 | `server.js:191` | لا إغلاق لطيف عند `SIGTERM`؛ Render يقتل العملية فتُقطع طلبات جارية. | متوسطة | `server.close()` ثم خروج، بمهلة قصوى | أُصلح — `d803e25` |
| 14 | `server.js:163-165` | `gzipSync` لمكتبة ٢٫٥ م.ب يُنفَّذ **في كل طلب**، وهو متزامن يوقف الحلقة على خطة مجانية. | متوسطة | تخزين النسخة المضغوطة في الذاكرة بعد أول مرة | أُصلح — `d803e25` |
| 15 | `app.src.jsx:382` | PDF محمي بكلمة مرور: `extractPdfText` تفشل، ويُبتلع الخطأ، فيُرسل الملف ويفشل التحليل برسالة غامضة من المزوّد. | متوسطة | رسالة عربية صريحة عند الرفع | أُصلح — `85e6a91` |
| 16 | `app.src.jsx:389` | صور آيفون `.heic` — وهي الصيغة الافتراضية في جوال القاضي — ترد برسالة «صيغة غير مدعومة» عامة بلا حل. | متوسطة | رسالة تسمّي الصيغة وتشرح كيف يحوّلها من الجوال | أُصلح — `08be7fc` |
| 17 | `app.src.jsx:847` | `start` يعتمد على `setBusy` غير المتزامن؛ ضغطتان سريعتان قد تبدآن تحليلين متوازيين على الرصيد نفسه. | متوسطة | حارس `useRef` متزامن | أُصلح — `d190c54` |
| 18 | `app.src.jsx:277,2014` | PDF مصوّر عند مزوّد لا يقبل PDF وفشل ترسيم صفحاته: تُرسل كتلة `image` بنوع `application/pdf` فيرفضها المزوّد برسالة غامضة. و«اسأل ملف القضية» لا يستدعي `prepareForProvider` أصلًا. | متوسطة | كتلة نصية صريحة بدل الكتلة المعطوبة، واستدعاء التهيئة قبل السؤال | أُصلح — `08be7fc` |
| 19 | `app.src.jsx` (متفرق) | أزرار أيقونية بلا `aria-label`: الإعدادات، الإغلاق، الحذف، إضافة الملفات. | متوسطة | `aria-label` عربي لكل زر أيقوني | أُصلح — `08be7fc` |
| 20 | `server.js:153-174` | لا `ETag` على الملفات الثابتة، و`app.js?v=2` رقم ثابت لا يتغير مع النشر. مع `no-cache` يُعاد تنزيل ٢٣٠ ك.ب في كل فتح. | متوسطة | `ETag` من زمن التعديل والحجم، ورد `304` عند التطابق | أُصلح — `d803e25` |
| 21 | `app.src.jsx:825-828` | `addFiles` يكتب الخطأ في متغير واحد، فإن فشل ملفان لا يرى القاضي إلا آخرهما. | متوسطة | جمع كل الأخطاء وعرضها معًا | أُصلح — `08be7fc` |
| 22 | `app.src.jsx:502` | تبديل الوضع إلى «كلمة مرور الموقع» لا يعيد اسم النموذج إلى نموذج كلود، فيبقى مثلًا `deepseek-chat` محفوظًا. | متوسطة | إعادة النموذج إلى أول نماذج كلود عند التبديل | أُصلح — `08be7fc` |
| 23 | `app.src.jsx:992-1013` | استيراد تحليل داخل قضية قائمة **يستبدل الدراسة بلا تأكيد**. (يُحتفظ بـ`prevAnalysis` لكن لا سبيل للقاضي لاستعادتها من الواجهة.) | متوسطة | **مؤجّل** — يحتاج قرار منتج: نافذة تأكيد، أم زر «استرجع الدراسة السابقة»؟ | مؤجّل — سؤال ٢ |
| 24 | `app.src.jsx:2065-2069` | `findFile` يطابق اسم المستند تقريبيًا؛ والنموذج يسمّي المستند بعنوانه الداخلي («صحيفة الدعوى») لا باسم الملف، فيظهر «لم يُطابق» كثيرًا. الرسالة تشرح لكن لا تحل. | متوسطة | **مؤجّل** — الحل الصحيح قائمة اختيار يدوي تُحفظ في القضية، وهو تغيير واجهة يحتاج إقرارك | مؤجّل — سؤال ٣ |
| 25 | `test/json.test.js` | لا تغطية لحالات: `NaN` و`Infinity` من النموذج، سلاسل بعلامة اقتباس مفردة، تعليقات `//`، أكثر من ٢٥ قوسًا قبل الـ JSON. | متوسطة | أُضيفت الاختبارات، وأُصلح ما ظهر منها | أُصلح — `031de93` |
| 26 | `app.src.jsx:54` | `uid()` سبعة محارف من `Math.random` — احتمال تصادم غير معدوم مع كثرة الملفات. | منخفضة | `crypto.randomUUID()` عند توفره | مؤجّل |
| 27 | `app.src.jsx:675` | البحث في القضايا `includes` حرفي: «إبراهيم» لا يطابق «ابراهيم». | منخفضة | تطبيع الألف والهمزة والتاء المربوطة قبل المقارنة | مؤجّل |
| 28 | `app.src.jsx:1555` | `usedIn` تمرّر مصفوفة من عنصر واحد إلى `findStatute`، فيسقط شرط اسم النظام وقد تُنسب مادة لمسألة ليست لها. | منخفضة | تمرير المكتبة كاملة ومقارنة الهوية | مؤجّل |
| 29 | `app.src.jsx:1852` | `buildMemo(c, a, library)` لا تستعمل `library` إطلاقًا. | منخفضة | حذف المعامل | مؤجّل |
| 30 | `app.src.jsx:2140-2145` | `highlight` تبحث بأول ٤٠ حرفًا من المقتبس ثم تظلل بطوله الكامل، فقد يتجاوز التظليل موضعه. | منخفضة | التظليل بطول ما طوبق فعلًا | مؤجّل |
| 31 | `src/json-repair.js:8-13` | `stripFences` يزيل ``` أينما وردت، ولو داخل مقتبس من مستند. | منخفضة | إزالتها في أول السطر فقط | مؤجّل |
| 32 | `src/json-repair.js:95` | `tried < 25`: إجابة فيها أكثر من ٢٥ قوسًا قبل الـ JSON الحقيقي تفشل. | منخفضة | البدء من آخر قوس أيضًا لا من أوله فقط | مؤجّل |
| 33 | `src/prompts.js:35` | `const S` معرّف في `stagePrompt` وغير مستعمل. | منخفضة | حذفه | مؤجّل |
| 34 | `index.html:5` | `viewport` بلا `viewport-fit=cover`؛ على آيفون ذي الحافة تبقى مساحة بيضاء. | منخفضة | إضافتها مع `env(safe-area-inset)` | مؤجّل |
| 35 | `server.js:180` | `catch` العام يعرض `e.message` الداخلي للعميل. | منخفضة | رسالة عامة، والتفصيل في السجل فقط | مؤجّل |

---

## ثانيًا: بنود النطاق التي فُحصت ووُجدت سليمة

| البند | ما فُحص | النتيجة |
|---|---|---|
| ١ | `npm install` / `build` / `test` | نظيف. ٦٥ ← ٨٢ اختبارًا بعد الإصلاحات. تحذير `npm audit` واحد متوسط على esbuild وهو **أداة بناء فقط لا تُشحن**، وترقيته كسر متعمَّد — مؤجّل بقرارك (سؤال ١) |
| ٢ | مسارات الخادم | `/` و`/app.js` و`/api` و`/healthz` ← ٢٠٠. `/server.js` و`/HANDOFF.md` و`/app.src.jsx` و`/package.json` و`/src/providers.js` و`/../server.js` ← ٤٠٤. `OPTIONS` و`PUT` على `/api` ← ٤٠٥ |
| ٣ | `package.json` و`.gitignore` | السكربتات الأربعة صحيحة، `engines: >=18`، **لا اعتماديات تشغيل** (esbuild devDependency وحده)، و`.gitignore` يستثني `node_modules` و`.env*` والسجلات |
| ٦ | `useEffect` | كل مؤقت واشتراك يُنظَّف: الخط في `App`، `setTimeout` التوست، `setInterval` العدّاد، راية `live` في `Viewer`. لا حلقات من مصفوفة اعتماديات خاطئة |
| ٧ | حذف القضية | يحذف **بالبادئة** `midad:file:<caseId>:` لا بقائمة القضية، فلا يبقى ملف يتيم. حد ٦ م.ب للملف و٢٤ م.ب للاستيراد يُطبَّق **قبل** القراءة |
| ١٠ | `repairJSON` | صامد أمام: أسوار markdown، نص قبل الـ JSON فيه `{`، قطع داخل قيمة نصية، مفتاح معلّق، محارف تحكم خام، فواصل زائدة، مصفوفة جذرية. (النواقص في البند ٢٥ وأُصلحت) |
| ١١ | طبقة المزودين | محوّلان يغطيان خمسة مزودين؛ بناء الطلب وقراءة الاستجابة وترجمة الأخطاء مغطّاة بـ١٢ اختبارًا، والقائمة البيضاء بـ٧ اختبارات تثبت رفض أي عنوان خارجها بلا إرسال شيء |
| ١٢ | «الحرفي فقط» | التوجيه يمنع التلخيص صراحة، والواجهة **تخفي فقرة `answer`** في الوضع الحرفي فلا يظهر إلا المقاطع الأصلية مظللة. و`notFound` يعرض الجملة الملزمة من `NOT_FOUND` حرفيًا بلا زيادة |
| ١٣ | عارض المستندات | تظليل الصفحة بعلامة `[صفحة N]`، وتظليل المقتبس، والعرض المزدوج، وزر التبديل بين النص المستخرج وPDF الأصلي. (تطابق الأسماء في البند ٢٤) |
| ١٤ | تصدير المذكرة | `.doc` بـ HTML مع `dir="rtl"` و`charset=utf-8` وBOM، وكل قيمة تمر بـ`esc()` فلا حقن. سجل النسخ يحفظ نسخة عند كل إنشاء، والمقارنة تعرض المضاف والمحذوف |
| ١٧ | أمن الواجهة | `dangerouslySetInnerHTML` في `mk()` **وحدها**، ومدخلاتها سلاسل أيقونات ثابتة في الكود. لا نص من المستخدم ولا من النموذج يمر بها. أسماء الملفات والمقتبسات تُعرض كنص عبر JSX |
| ١٨ | الأداء | ملاحظة القاضي تُصيّر بطاقتها وحدها لا الشاشة، والكتابة في IndexedDB مؤجّلة ٦٠٠ مللي ثانية لا عند كل حرف. `app.js` ٢٢٨ ك.ب |
| ٢٠ | القائمة البيضاء للملفات | `PUBLIC_FILES` أربعة مسارات معدودة؛ أي مسار آخر ٤٠٤ قبل أي لمس للقرص. لا اجتياز مسارات |
| ٢١ | القائمة البيضاء للمزودين | `upstreamFor` يبني العنوان من الجدول لا من الترويسة، ويرفض `direct` ويرفض ما ليس في `PROVIDER_LIST`. مغطّاة باختبار يمرر `https://attacker.example/v1` و`http://127.0.0.1:1/x` ويثبت أن شيئًا لم يُرسل |
| ٢٢ | حجم الجسم والمهلة | ٦٤ م.ب حدًّا مع `req.destroy()`، ومهلة ٢٨٠ ثانية بـ`AbortController`، و`send()` يمتنع إن أُغلق الاتصال. (الإغلاق اللطيف في البند ١٣) |
| ٢٤ | تسريب الأسرار | لا مفتاح ولا كلمة مرور في أي `console.log`؛ سطر الإقلاع يطبع `set/MISSING` لا القيمة. `scrub()` ينقّي المفتاح من كل نص خطأ، ويغطيه اختبار يردّده المزوّد عمدًا |
| ٢٦ | `/healthz` | موجود ويرد `ok` بلا مصادقة، صالح لفحص Render |
| ٢٧ | القواعد اللغوية | `SYS` يمنع «ثابت/يثبت/متفق عليه/ثبت» ويمنع النتيجة والترجيح والتوصية وتقييم قوة الدليل، ويلزم `src` بمستند وصفحة ومقتبس. مطابق لـ`HANDOFF.md` حرفًا بحرف |
| ٢٨ | النصوص النظامية | `caseStatutes` هي المصدر الوحيد، والافتراضي لا شيء. توجيه مرحلة المسائل يقول: «لا تستشهد بأي مادة من خارج هذه القائمة مهما كانت معرفتك بها»، وعند الخلو يُكتب «(لا توجد نصوص مضافة)» ويبقى القسم فارغًا بصراحة |
| ٢٩ | تطابق المخططات | فُحص حقلًا حقلًا: كل حقل يطلبه التوجيه يقرأه التطبيق، وكل حقل يقرأه التطبيق يطلبه التوجيه. لا زائد ولا ناقص |
| ٣٠ | حقول القاضي | فُحص كل مسار يكتب في `notes` و`direction`: `JudgeNote.onChange` و`textarea` اتجاه الدراسة **فقط**. `runStages` و`readAnalysis` و`importedCase` لا تكتب فيها حرفًا؛ الأولى تُهملها والثانيتان تبدآنها فارغة |
| ٣١ | `index.html` | `noindex, nofollow` في الوسم وفي رأس `X-Robots-Tag`، و`viewport` موجود، وأيقونة SVG مضمّنة، وشبكة أمان تعرض رسالة عربية إن لم تُحمَّل React |
| ٣٣ | `test-fixtures` | ثلاثة ملفات Word صالحة وقائمة تحقق مفصّلة تذكر بالضبط ما يجب أن يلتقطه التطبيق (الأرقام والتواريخ والتعارضات المزروعة)، و`analysis.json` جاهز لتجربة الاستيراد |

## ثالثًا: ما لم يكن ممكنًا فحصه من هنا

| البند | السبب |
|---|---|
| ١٦ (جزئيًا) | فُحص عرض ٣٧٥px آليًا في Chromium: لا تمرير أفقي في الشاشات العشر، والأزرار ≥ ٤٤px، والشريط السفلي لا يغطي المحتوى (`pb-40`). لكن **سلوك لوحة مفاتيح سفاري على iOS لا يُحاكى**؛ وضعته في قائمة الاختبار اليدوي |
| ١٩ (جزئيًا) | التباين والـ`focus` فُحصا بالقيم؛ قارئ الشاشة الحقيقي (VoiceOver) يحتاج جهازًا |
| ٣٢ | `README.md` و`HANDOFF.md` حُدّثا بعد الإصلاحات، والتحقق النهائي من مطابقتهما للواقع على Render بيدك بعد النشر |

---

## رابعًا: قائمة اختبار يدوي — عشر خطوات على سفاري في الجوال

نفّذها بعد النشر، بملفات `test-fixtures` **لا بقضية حقيقية**.

| # | الخطوة | ما يجب أن يحدث |
|---|---|---|
| ١ | افتح `midad-al-qadi.onrender.com`. اضغط **⚙️** ← تبويب **كلمة مرور الموقع** ← أدخلها ← **احفظ واختبر الاتصال** | يظهر العدّاد بالثواني، ثم **«الاتصال يعمل»**. أول فتح قد يتأخر نحو دقيقة لأن الخادم المجاني ينام |
| ٢ | **ابدأ العمل** ← **قضية جديدة** ← نوع الدعوى **تجارية** ← ارفع `صحيفة-الدعوى.docx` و`المذكرة-الجوابية.docx` ← **ابدأ التحليل** | تظهر المراحل الست واحدة بعد أخرى، وتحتها زر **أوقف التحليل**. اضغطه في المرحلة الثانية: يقف فورًا وتُفتح القضية بما اكتمل |
| ٣ | من القائمة اضغط **أعد التحليل كاملًا** ودعه يكمل | تكتمل الست، ثم **«اكتمل ترتيب الملف»** |
| ٤ | افتح **النظرة العامة** واضغط **تعديل** على بطاقة مسألة | يجب أن يظهر زرا **تعديل** و**حذف** بلا حاجة إلى ضغط مطوّل، ويُفتح حقل الكتابة |
| ٥ | اكتب في **ملاحظة القاضي** تحت أي طلب، ثم **اخرج من المتصفح فورًا** وارجع | الملاحظة موجودة كما كتبتها |
| ٦ | اضغط **اكتشف التعارضات** | يمسك اختلاف المبلغ (٨٥٠٬٠٠٠ / ٩٢٠٬٠٠٠) واختلاف تاريخ التسليم، ومعه **عرض المصدرين** |
| ٧ | من الشريط السفلي اسأل: **«هل يوجد شرط جزائي في العقد؟»** | يجب أن يرد بالجملة: **«لم أعثر في ملفات القضية المرفوعة على ما يثبت ذلك.»** بلا زيادة ولا تخمين |
| ٨ | فعّل **الحرفي فقط** واسأل: **«أين ذكر تاريخ التسليم؟»** | تظهر المقاطع الأصلية مظللة بلا فقرة تلخيص، وتحت كل مقطع مصدره |
| ٩ | اضغط سطر **المصدر** تحت أي واقعة | يفتح العارض من اليسار، ويظلّل الصفحة والمقتبس. جرّب **افتح مصدرًا آخر بجانبه** |
| ١٠ | **النصوص النظامية** ← **استورد الأنظمة المرفقة** ← أشّر على نظام ← ثم **مذكرة الدراسة** ← **إنشاء مذكرة دراسة** ← **تصدير Word** | تنزل المذكرة `.doc` وتفتح بعربية سليمة من اليمين لليسار، و**«اتجاه الدراسة»** في آخرها **فارغ** |

**وأثناء ذلك راقب ثلاثة أشياء:** ألا تحتاج إلى تمرير الشاشة يمينًا ويسارًا في أي قسم، وألا تحجب لوحة المفاتيح حقل السؤال حين تكتب فيه، وأن كل زر تضغطه بإبهامك يستجيب من أول ضغطة.
