# مِداد القاضي — وثيقة تسليم كاملة (Handoff)

> اقرأ هذا الملف كاملًا قبل أي تعديل. هو كل ما تحتاج معرفته عن المشروع، وحالته الحالية، وما هو مطلوب منك الآن.
> صاحب المشروع: عبدالله — قاضٍ سعودي، رئيس محكمة جزائية، ومحامٍ وباحث دكتوراه. يعمل من الجوال غالبًا، ويفضّل شرحًا عربيًا بسيطًا وتعليمات دقيقة (افعل كذا، ثم كذا). خاطبه بالعربية.

---

## 1. ما هو المشروع

**مِداد القاضي**: مساحة عمل ذكية لدراسة ملف القضية. أداة شخصية للقاضي تُنظّم ملف القضية ولا تحكم فيه.

**المبدأ الأساسي (غير قابل للتفاوض):** الذكاء الاصطناعي هنا يُنظّم ويستخرج ويربط ويلخّص ويكشف التناقض ويساعد في البحث، ولا يحل محل السلطة التقديرية للقاضي. يجب أن يشعر المستخدم أن النظام "يعيد ترتيب ملف القضية أمامه، ولا يفكر بدلًا عنه".

**قواعد لغوية ملزمة في كل نص يظهر على الشاشة أو يخرج من النموذج:**
- لا "يثبت" ولا "ثابت" ولا "متفق عليه". بدلها: "ورد في المستند"، "ادعى الطرف"، "نازع الطرف"، "لم يظهر في الملف نزاع صريح".
- لا نتيجة قضائية، لا ترجيح، لا توصية بالحكم، لا تقييم لقوة دليل.
- كل معلومة مستخرجة تحمل مصدرها: المستند — الصفحة — مقتبس حرفي.
- حقول "ملاحظة القاضي" و"اتجاه الدراسة" لا يكتب فيها الذكاء الاصطناعي حرفًا واحدًا.
- النصوص النظامية: لا يستشهد النموذج بأي مادة إلا ما أضافه القاضي بنفسه في قسم "النصوص النظامية". إن لم يُضف شيئًا، يبقى القسم فارغًا بصراحة.
- إن لم يجد النموذج إجابة في الملف يكتب حرفيًا: "لم أعثر في ملفات القضية المرفوعة على ما يثبت ذلك."
- مؤشر التقدم اسمه "اكتمال تنظيم الدراسة" وليس اكتمال القضية.
- **بيانات وهمية فقط.** لا يُرفع ملف قضية حقيقية إلى هذه النسخة أبدًا.

المواصفات الكاملة للمنتج في `SPEC.md` في هذا المستودع.

---

## 2. الحالة الحالية (منشور ويعمل)

| البند | القيمة |
|---|---|
| الرابط العام | https://midad-al-qadi.onrender.com |
| المستودع | https://github.com/aalmuataq19-hash/midad (عام/Public، فرع `main`) |
| الاستضافة | Render، Web Service، خطة Free، منطقة Frankfurt |
| معرّف الخدمة | `srv-dae0hbu7bikc73fl80h0` |
| لوحة Render | https://dashboard.render.com/web/srv-dae0hbu7bikc73fl80h0 |
| مساحة العمل في Render | `tea-dadvr8nqj5pc739n3h60` |
| أمر البناء في Render | `npm install` |
| أمر التشغيل | `node server.js` |
| النشر التلقائي | **معطّل حاليًا** (GitHub غير مربوط بـ Render). بعد كل دفع اطلب النشر يدويًا، أو اربطه بحسب `README.md` |
| Node | 18+ (Render يستخدم 26) |

**متغيرات البيئة في Render (مضبوطة فعلًا، لا تضعها في الكود أبدًا):**

| المتغير | الحالة |
|---|---|
| `ANTHROPIC_API_KEY` | مضبوط (مفتاح من console.anthropic.com، رصيد 10$) |
| `ACCESS_PASSWORD` | مضبوط (كلمة مرور الدخول للموقع) |
| `ALLOWED_MODELS` | `claude-sonnet-5,claude-opus-5,claude-fable-5-1,claude-haiku-4-5-20251001,claude-sonnet-4-6` |
| `MAX_TOKENS` | `8000` |
| `RATE_LIMIT_PER_MINUTE` | `30` |

**تنبيه أمني:** المستودع عام. لا تكتب فيه أي مفتاح أو كلمة مرور. كل الأسرار في Render فقط.

**التحقق السريع:** `https://midad-al-qadi.onrender.com/api` يجب أن يعرض `{"ok":true,"midad":true,"configured":true}`.

---

## 3. ما أُنجز في جولة المراجعة الأخيرة

خط البناء وُثّق واختُبر، وروجعت الشيفرة كاملة وأُصلح ما وُجد فيها من مسارات تُعطّل التطبيق:

| ما كان يحدث | الإصلاح |
|---|---|
| فشل قبل بدء المراحل (ملف معطوب، ذاكرة ممتلئة) يترك شاشة «يعمل مِداد على ترتيب الملف…» عالقة للأبد | `try/finally` في `start` و`reanalyze`، والقضية تُفتح على كل حال بملفاتها |
| ملفات تُحفظ في IndexedDB قبل تسجيلها في القضية، فتبقى بلا مالك ولا يحذفها حذف القضية | تُسجَّل الملفات في القضية أولًا، والحذف صار بالبادئة `midad:file:<caseId>:` لا بقائمة القضية |
| ملف Word بلا نص يُرسل كمستند فارغ فتفشل المراحل الست كلها | يُرفض عند الرفع برسالة واضحة، ويُحيَّد في `fileBlocks` |
| `repairJSON` يسقط على: نص قبله فيه `{`، قطع داخل قيمة نصية، سطر جديد حقيقي داخل قيمة | أُعيدت كتابته في `src/json-repair.js` ويغطيه 28 اختبارًا |
| قيمة معطوبة في متغيرات بيئة Render ترسل `max_tokens: NaN` أو `model: undefined` | كل قيمة رقمية لها قيمة احتياطية، والقائمة الفارغة ترجع للافتراضي |
| كتابة رد مرتين في `server.js` تُسقط الخدمة على Render | `send()` يمتنع، ومعالجات على مستوى العملية تسجّل ولا تُسقط |

**من الآن: أي تعديل على الواجهة يكون في `app.src.jsx` ثم `npm run build` ثم commit للملفين معًا.** لا تعدّل `app.js` يدويًا أبدًا.

### الباقي على عبدالله
1. ربط GitHub بـ Render (أو ضبط سر `RENDER_DEPLOY_HOOK`) — التفاصيل في `README.md`.
2. اختبار حقيقي بملفات `test-fixtures/` بحسب قائمة التحقق في `test-fixtures/README.md`.
3. تدوير مفتاح API وجعل المستودع خاصًا.

## 4. البنية التقنية

```
midad/
├── index.html               صفحة واحدة. تحمّل React 18 وTailwind (Play CDN) وmammoth من CDN، ثم app.js
├── app.js                   التطبيق المترجم (لا يُعدَّل يدويًا)
├── app.src.jsx              المصدر (React + JSX). هنا تُعدَّل الواجهة
├── src/json-repair.js       قراءة JSON من إجابة النموذج وإصلاحها. يضمّه esbuild في app.js
├── server.js                خادم Node بلا اعتماديات: يقدّم index.html وapp.js فقط، ووسيط /api
├── test/                    اختبارات وحدة بـ node --test (قراءة JSON + تمرير الوسيط)
├── test-fixtures/           قضية وهمية بثلاثة ملفات Word + قائمة تحقق
├── .github/workflows/       نشر تلقائي على Render عبر Deploy Hook
├── package.json             start + build + test
├── SPEC.md                  مواصفات المنتج الكاملة
├── HANDOFF.md               هذا الملف
└── README.md                دليل النشر المختصر
```

**الأوامر:** `npm install` ثم `npm run build` (يبني app.js من app.src.jsx بـ esbuild مع `--bundle`) و`npm test` (اختبارات قراءة JSON).

### server.js (الوسيط)
- `GET /api` → `{ok, midad, configured}` (فحص).
- `POST /api` → يتحقق من رأس `x-midad-pass` ضد `ACCESS_PASSWORD`، يحد الطلبات لكل IP، يقيّد `model` بالقائمة المسموحة و`max_tokens` بالحد الأعلى، ثم يمرّر الطلب إلى `https://api.anthropic.com/v1/messages` مع `x-api-key` من البيئة، ويعيد الاستجابة كما هي.
- يحذف من الجسم قبل تمريره: `stream` و`metadata`، و`temperature` و`top_p` و`top_k` (دفاعيًا، لأن النماذج الحديثة ترفضها ولئلا يفشل متصفح بقي على نسخة قديمة من `app.js`).
- أخطاؤه الخاصة بصيغة Anthropic: `midad_auth` (401)، `midad_config` (500)، `midad_rate` (429)، `midad_request` (400/413)، `midad_upstream` (502).
- يقدّم فقط `index.html` و`app.js` (قائمة بيضاء). البقية 404.
- `Cache-Control: no-cache` على الملفات العامة حتى لا يبقى المتصفح على نسخة قديمة.
- مهلة الطلب 300 ثانية لأن التحليل قد يطول.

### app.src.jsx (الواجهة)
- **وضعان للاتصال** يكتشفهما تلقائيًا عند التحميل عبر `GET api?ping=1`:
  - `proxy`: عبر `/api` بكلمة مرور (الافتراضي على Render).
  - `direct`: المتصفح → Anthropic مباشرة بمفتاح API شخصي (يُستخدم حين لا يوجد خادم، أو باختيار المستخدم).
  - مسار الوسيط قابل للضبط بـ `window.MIDAD_API` (في Hostinger كان `api.php`).
- **التخزين:** IndexedDB في متصفح المستخدم (`midad-al-qadi` / store `kv`). المفاتيح: `midad:cases` (كل القضايا بلا ملفات)، `midad:file:<caseId>:<fileId>` (كل ملف بمحتواه base64 ونصه المستخرج)، `midad:library` (الأنظمة والمبادئ). الإعدادات في localStorage: `midad:key`، `midad:pass`، `midad:model`، `midad:forceDirect`.
  - نتيجة ذلك: القضايا لا تنتقل بين جهازين، ولا يوجد شيء في الخادم. مقصود.
- **قراءة الملفات:** PDF (pdf.js من cdnjs يستخرج النص مع علامات `[صفحة N]`؛ إن كان مصوّرًا بلا نص يُرسل الملف نفسه base64)، Word docx (mammoth)، صور (png/jpg/webp/gif)، نص. الحد 6 ميجابايت للملف.
- **التحليل في 6 مراحل متتابعة** (`STAGES`)، كل مرحلة طلب مستقل يُرسل كل الملفات + prompt خاص، ويُعيد JSON بمخطط محدد:
  1. `overview`: صورة القضية، الأطراف، المسائل (≤6)، بيانات القضية.
  2. `facts`: الوقائع (≤12) مع `date` ISO إن تأكد التحويل و`date_text` كما ورد، `kind` (document|claimed|disputed)، `conf` (high|low)، `src`.
  3. `reqdef`: طلبات المدعي (`pl` مع `replied`)، طلبات المدعى عليه (`df`)، الدفوع (`defenses`).
  4. `evidence`: جدول المستندات؛ عمود الغرض يبدأ دائمًا بـ "يراد به بحسب المذكرة".
  5. `issues`: لكل مسألة pl/df/evidence/laws/questions؛ laws فقط من النصوص التي أضافها القاضي (تُمرَّر في الـ prompt).
  6. `review`: التعارضات (≤6) وما يحتاج إلى تحقق (≤8). يُنبَّه النموذج أن الهجري والميلادي لنفس اليوم ليسا تعارضًا، والأرقام العربية والهندية كذلك.
  - **لا يُرسل `temperature`**: ألغته النماذج الحديثة (Sonnet 5 وOpus 5 وFable 5.1) وترفض الطلب الذي يحمله برسالة `temperature is deprecated for this model`. لا تُعده. وكذلك `top_p` و`top_k`.
  - `max_tokens` 8000 للمراحل، 4000 لسؤال الملف. رُفعت لأن التفكير (thinking) صار مفعّلًا افتراضيًا في هذه النماذج ويستهلك من `max_tokens`، فالحد الضيق يقطع JSON في منتصفه ويفقد وقائع بصمت. السقف في Render `MAX_TOKENS=8000`.
  - لا تضف `output_config.effort`: غير مدعوم على Haiku 4.5 وهو أحد خيارات القائمة، فيكسره.
  - **تخزين مؤقت (prompt caching):** `fileBlocks` تضع `cache_control: {type:"ephemeral"}` على آخر كتلة قبل نص المرحلة، فتُخزَّن البادئة (النظام + كل الملفات) في المرحلة الأولى وتُقرأ في الخمس الباقية وفي أسئلة الملف. البادئة ثابتة والمتغير الوحيد هو النص بعد النقطة. المدة 5 دقائق تتجدد مع كل قراءة. الحد الأدنى للتخزين يعتمد على النموذج (512 رمزًا على Opus 5، و1024 على Sonnet 5، و4096 على Haiku 4.5)؛ الملف الأصغر من ذلك لا يُخزَّن بلا خطأ. `server.js` يمرّر الحقل كما هو، ويغطيه `test/proxy.test.js`.
  - كل مرحلة تفشل تُسجَّل في `analysis.errors[key]` وتُعرض رسالتها تحت المرحلة، مع زر "أعد التحليل".
- **قراءة JSON** (`repairJSON` → `pj` → `pjOrFix`): تُزال أسوار markdown، يُمشى على النص مع تتبّع الأقواس والسلاسل، وإن كان مقطوعًا يُقطع عند آخر عنصر مكتمل وتُغلق الأقواس وتُزال الفواصل الزائدة. إن فشل كل ذلك، يُرسل النص للنموذج بـ `FIX_SYS` ليُصلحه. هذه آخر إصلاح تم (بسبب `JSON Parse error: Expected ']'`).
- **الأقسام العشرة:** النظرة العامة (صورة القضية، خريطة الأطراف، المسائل قابلة للتعديل)، الوقائع (بطاقات + نص أصلي + وسم ثقة)، الخط الزمني (مع تعارضات التاريخ)، الطلبات (عمودان + "قارن المذكرات")، الدفوع، الأدلة (جدول قابل للفرز)، المسائل (5 أقسام + "اربط مبدأ" + "عرض الطرفين" + أسئلة مقترحة تُختار لوضع الجلسة)، النصوص النظامية (تبويبان: الأنظمة / مكتبتي الشخصية)، ملاحظاتي (+ قائمة متابعة + وضع الجلسة)، مذكرة الدراسة (+ تصدير Word كـ .doc HTML + سجل النسخ مع فرق).
- **خصائص أفقية:** اسأل ملف القضية (شريط سفلي، وضع "الحرفي فقط")، اكتشف التعارضات (عرض المصدرين جنبًا إلى جنب)، ما الذي يحتاج إلى تحقق (تحويل لمهمة)، ما الجديد (مقارنة تحليل قديم بجديد عند إضافة ملفات)، عارض المستندات (لوحة يسارية، نص مستخرج مع تظليل الصفحة والمقتبس، أو PDF أصلي)، حذف القضية نهائيًا مع ملفاتها.
- **التواريخ:** `dual(iso, text)` تعرض الميلادي والهجري (أم القرى) معًا عبر `Intl`.
- **الأيقونات:** SVG داخلية (`mk(...)`) لا lucide، لأن النسخة مستقلة عن أي bundler.

### نظام التصميم (لا تغيّره دون طلب صريح)
- خط: IBM Plex Sans Arabic (Google Fonts). عناوين 600–700، نص 400–500، ارتفاع سطر واسع.
- ألوان: خلفية `#FAF9F6`، بطاقات `#FFFFFF`، نص `#1A1A1A`، ثانوي `#8A8378`، حدود `#E8E4DD`، اللون التفاعلي الوحيد `#1F4D3D`، "يحتاج مراجعة" `#B8860B`، تعارض `#A0522D`، تظليل `#FFF3C4`، خلفية ملاحظة القاضي `#FBF8EF` بحد منقّط.
- زوايا 8–12px، ظلال شبه معدومة، حدود رفيعة، حركة ≤250ms، RTL كامل.
- يجب أن يبدو "منتجًا تقنيًا عربيًا فاخرًا صُمم في 2026"، لا لوحة إدارة حكومية. Progressive disclosure: المهم أولًا.
- كل صندوق يكتبه القاضي متميز بصريًا عن محتوى الذكاء الاصطناعي.

---

## 5. تاريخ القرارات المهمة (لماذا الأشياء كما هي)

1. بدأ المشروع كوثيقة مواصفات، ثم حُوّل إلى تطبيق React داخل Claude Artifacts. فشل الاتصال بالنموذج من داخل تطبيق الجوال (خيار "AI-powered artifacts" غير متاح هناك)، فتُركت هذه النسخة.
2. بُنيت نسخة مستقلة (HTML واحد) تعمل بمفتاح API شخصي في أي متصفح.
3. جُهّزت حزمة لـ Hostinger (PHP proxy `api.php` + `.htaccess`) لكن Hostinger لا يمكن الربط بها آليًا، فاختار عبدالله Render.
4. حُوّل الوسيط إلى Node (`server.js`) ونُشر على Render من مستودع GitHub رفعه عبدالله يدويًا من الجوال.
5. أول اختبار حقيقي: الاتصال يعمل، لكن المرحلة الأولى فشلت بـ `JSON Parse error`. أُصلح بمُصلح JSON ثلاثي الطبقات + حرارة صفر (هذه النسخة).
6. ملاحظة تصميمية من مراجعة خارجية: "ما تثبته المستندات بذاتها" ليست طبقة محايدة بل تقدير للدليل؛ لذا التسمية في التطبيق "ما ورد في المستندات نصًا". وكذلك "متفق عليه" صارت "لم يظهر في الملف نزاع صريح" لأن الاتفاق في المذكرات قد يكون إقرارًا له أثر.

---

## 6. المتراكم (Backlog) بترتيب الأولوية

1. **اختبار شامل بملفات وهمية** وإصلاح ما يظهر (الأهم الآن).
2. **ربط GitHub بـ Render** ثم جعل المستودع خاصًا (Private). حاليًا عام.
3. **تدوير مفتاح API**: المفتاح الحالي مرّ عبر محادثة؛ يُنشأ مفتاح جديد في console ويُحدَّث `ANTHROPIC_API_KEY` في Render ويُحذف القديم.
4. **Tailwind ثابت** بدل Play CDN (توليد CSS في البناء) لتسريع التحميل وإزالة تحذير الإنتاج.
5. **قوالب بحسب نوع القضية** أعمق من مجرد سطر وصفي: أقسام خاصة للجزائية (لائحة الادعاء، محضر التحقيق، الإقرار وظروفه، سلسلة الأدلة، المضبوطات) والعمالية والأحوال الشخصية والتنفيذ. عبدالله يرأس محكمة جزائية، فهذه أهم من المثال التجاري.
6. **تحسين استخراج التاريخ الهجري** والأرقام العربية الهندية في المبالغ.
7. **OCR للمستندات المصوّرة وخط اليد**: حاليًا تُرسل الصورة/الـPDF للنموذج مباشرة؛ يُقاس الأداء ويُحسَّن.
8. **مجموعة اختبار مرجعية**: 5–10 قضايا وهمية مفهرسة يدويًا لقياس دقة الاستخراج بعد كل تغيير.
9. **الأدوار**: من يستخدم النظام غير القاضي (مساعد، كاتب ضبط)؟ يحدد الصلاحيات لاحقًا.
10. **الجوال**: نسخة قراءة فقط تعرض المسائل والملاحظات والمهام.
11. **خطة Render**: Free تنام بعد 15 دقيقة خمول (أول طلب بعدها يتأخر ~دقيقة). Starter يلغي ذلك حين يستقر الاستخدام.
12. **نطاق مخصص** بدل onrender.com حين يريد.

---

## 7. كيف تختبر

1. أنشئ في Word ملفين وهميين بأسماء مخترعة: صحيفة دعوى (صفحتان) ومذكرة جوابية، وضع فيهما مبلغين مختلفين للمطالبة عمدًا وتاريخين مختلفين لنفس الحدث.
2. افتح الموقع → كلمة مرور الموقع → احفظ واختبر الاتصال → ابدأ العمل → + قضية جديدة → ارفع الملفين → ابدأ التحليل.
3. بعد التحليل: اكتشف التعارضات (يجب أن يمسك فرق المبلغ والتاريخ)، ما يحتاج تحقق، اسأل الملف بسؤال جوابه غير موجود (يجب أن يرد بالجملة الحرفية "لم أعثر…").
4. أضف مذكرة ثالثة فيها دفع جديد من "أضف ملفات إلى القضية" → يجب أن تظهر لوحة "ما الجديد".

---

## 8. أشياء أخرى موجودة خارج هذا المستودع (للعلم)

- حزمة Hostinger (`api.php` + `.htaccess` + `config.sample.php`) جاهزة إن أراد العودة إليها؛ الواجهة نفسها مع `window.MIDAD_API = "api.php"`.
- نسخة Claude Artifact (React بـ lucide-react وwindow.storage) — متروكة.
- وثيقة "أمر بناء" لأداة ChatGPT Sites (واجهة ببيانات ثابتة) — متروكة.

---

## 9. قواعد التعامل مع عبدالله

- عربية بسيطة، تعليمات دقيقة ومرقمة، بلا مصطلحات غير مشروحة.
- هو على الجوال غالبًا: أي خطوة يدوية اشرحها بأزرار الشاشة (اضغط X ثم Y).
- لا تعدّل التصميم أو المبادئ اللغوية من تلقاء نفسك.
- قبل أي تغيير كبير اعرض الخطة في سطرين ثم نفّذ.
- ذكّره بلطف: بيانات وهمية فقط، ولا تشارك المفتاح.
